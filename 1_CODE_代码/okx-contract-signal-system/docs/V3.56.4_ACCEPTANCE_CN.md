# OKX 合约信号系统 v3.56.4 最终验收

日期：2026-06-18

## 输入包核验

重新上传的同名 v3.56.3 ZIP 与其 SHA-256 文件一致：

`3541a5001f5b1419d26c53a2446d43ee89a806ae0405b0b4ace070445ae44e7b`

但它与此前通过验收的 v3.56.3 最终包并不相同。此前包 SHA-256 为：

`df2d9a9c80bb0b397e73e209535e3fc0c90ed069bdaf7251c43fb2bca4593151`

差异不是文件名后缀造成的：重新上传包有 169 个条目，已验收包有 171 个条目，并有 29 个同名文件内容发生变化。

## 整合原则

v3.56.4 以上一份已通过严格门控和干净解压验收的 v3.56.3 为基线，只吸收重新上传包中经过反例验证的有效变化。

保留：

- SQLite 生命周期和通知 outbox 连接确定关闭。
- WebSocket 已连接、未降级且扫描状态新鲜时，如闭合回补完整且仍在有效周期内，可将消息静默解释为闭合 K 线间隔，而不是立即误报 stale。
- Dashboard 保留 5m closed-backfill 兼容字段。

拒绝：

- 调度器将正式推送门写死为允许。
- approved manifest 只校验哈希而不校验正式研究、盲测封存、覆盖、操作者和时间顺序。
- daily learning 写入正式候选路径。
- 调度器不持续消费 outbox。
- 完整数据因非必要缓存写入失败被整体标记 failed。
- Dashboard 移除运行模式、manifest 原因、outbox 统计、UTF-8/no-store 和最新运行文件优先级。
- 移除 Next.js 内嵌 PostCSS 安全 override。

## 验收结果

- Python：338 passed，18 skipped，0 failed。
- 18 项跳过仅因为未配置 JIAOYI_DATA_DIR。
- Node：13 passed，0 failed。
- ESLint：通过且无警告。
- TypeScript：通过。
- Next.js 生产构建：通过。
- npm audit --omit=dev：0 vulnerabilities。
- 正式 manifest 缺失或语义不合格时继续 fail-closed。
- 正式入口不导入训练、回测或 ML 决策模块。
- 发布包不包含 .env、outputs、SQLite、日志、缓存、node_modules 或 .next。

## 结论

重新上传的同名 v3.56.3 包不能直接覆盖此前最终包。v3.56.4 是保留最新运行稳定性收益、同时恢复并锁定正式运行安全闭环的整合版本。
