# OKX 合约信号系统 v3.56.4 同名包差异整合说明

日期：2026-06-18

本版本以上一份已通过干净解压验收的 v3.56.3 为基线。用户重新上传的同名 ZIP 校验值为 `3541a5001f5b1419d26c53a2446d43ee89a806ae0405b0b4ace070445ae44e7b`，与已验收包 `df2d9a9c80bb0b397e73e209535e3fc0c90ed069bdaf7251c43fb2bca4593151` 不一致，且包含 29 个内容变化、3 个旧包独有文件和 1 个新包独有文件。

保留的有效变化：

- SQLite 生命周期/outbox 连接在事务结束后确定关闭。
- WebSocket 已连接但在闭合 K 线间隔内安静时，可使用新鲜且完整的 closed-backfill 状态作为市场数据存活证据。
- 保留 `closed_backfill_5m` 兼容字段。

拒绝的回退：

- 调度器硬编码允许推送。
- approved manifest 退化为仅哈希校验。
- daily learning 覆盖正式 `candidate_params.json`。
- 调度器取消持续 outbox drain。
- 15m 完整数据因非必要写入失败被整体标记失败。
- Dashboard 移除运行模式、manifest 原因、outbox 统计、UTF-8/no-store 和最新状态优先级。
- 移除 PostCSS 安全 override。

因此同名重上传包不能直接替代已验收包；v3.56.4 是两者经过反例审计后的安全整合版本。
