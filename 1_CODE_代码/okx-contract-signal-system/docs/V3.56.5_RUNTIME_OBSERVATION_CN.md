# OKX 合约信号系统 v3.56.5 实际运行观察报告

日期：2026-06-18

## 启动状态

- 启动入口：`py -3.12 main.py --cli`
- Dashboard：`http://127.0.0.1:3001`
- 系统版本显示：`v3.56.5`
- 运行模式：`SIGNAL_ONLY` / `research_observation`
- 交易所连接：仅使用 OKX 公共行情，不接入下单接口。

## 实际运行快照

- Dashboard API：可访问，返回 UTF-8 JSON 且 no-store。
- API 快照时间：`2026-06-18 17:52:42 北京时间`。
- 最新扫描时间：`2026-06-18 17:52:01 北京时间`，API 读取时 age 约 41 秒。
- `runtime_status`：`online`
- `runtime_reason`：`live_status_fresh`
- `runtime_status_source`：`latest_scan_status.json`
- OKX WebSocket：已连接，未降级。
- WebSocket 订阅：21 个 USDT-SWAP 币种，重启后 `reconnect_count=0`。
- 15m closed backfill：21/21 complete，`write_failures=0`。
- 5m runtime-cache closed backfill：21/21 complete，`write_failures=0`。
- 5m 兼容字段：`closed_backfill_5m` 已返回。
- Dashboard stale-symbol：`0`。
- SQLite outbox：`pending=0`，`sent=14`，`failed=0`，`dead_letter=0`，无积压。
- 正式推送：`push_allowed=false`。
- 推送拦截原因：`runtime_manifest_missing`，属于 approved manifest 缺失时的预期 fail-closed。

## 本轮修复

- 实际运行中发现 Dashboard 顶层 `quality.stale_symbols` 仍沿用旧的启动质检文件，运行态已在线且 closed backfill 完整时仍显示 21 个过期币种。
- v3.56.5 将 Dashboard stale-symbol 口径改为运行态优先：运行态在线时按当前 symbol rows、closed backfill 和实际行年龄计算；运行态离线、过期或错误时才回退到启动质检 stale 列表。
- 修复后，当前运行态在线、15m/5m closed backfill 均完整时，Dashboard stale-symbol 数量应为 0。

## 判断

当前系统模块联动正常：

- 主程序能够启动并获取运行锁。
- OKX WebSocket 成功连接并完成 21 币种订阅。
- 15m 信号周期与 5m 面板缓存均由 closed-candle backfill 驱动，且写入失败不会误判数据完整性。
- Dashboard 使用最新运行快照和 closed-backfill 状态作为权威数据源，不再让旧启动质检 stale 列表覆盖在线运行结果。
- 通知 outbox 可持续 drain，当前没有积压、失败或死信。
- approved manifest 缺失时系统继续观察行情和维护缓存，但正式 Feishu 信号推送保持 fail-closed。

## 仍需真实环境长期观察

- 继续运行多个完整 15m 周期，观察 OKX WebSocket 静默、断线重连和 REST closed 修复。
- 配置真实 strict-research `outputs/runtime/approved_strategy_manifest.json` 后，再验证正式 A/B 推送链路。
- 真实盈利能力不由运行健康状态证明，仍需历史检验、模拟观察和小规模验证。
