# OKX 合约信号系统 v3.56.11 发布说明

## 本次修复

- 修复桌面面板固定只显示 3 行造成的误解。面板现在明确显示配置总数，并可滚动查看全部 21 个币种。
- 新增 21 币种一致性门禁：配置、扫描状态、OKX WebSocket 订阅和闭合 K 线回补明细必须完全一致。
- 将预检、运行健康、影子组合和研究候选门禁统一到 `scripts/system_check.py`，正式发布包不再携带三个重复检查脚本。
- 正式发布包移除旧下单监控、旧 ML、重复通知、Streamlit 页面、每日自动学习等非主链模块。
- 移除 `plotly`、`streamlit` 及其无关传递依赖，保留信号系统实际需要的最小 Python 依赖。
- 健康检查优先使用带币种明细的回补证据；历史死信保留审计，但只有最近 24 小时的新死信阻塞当前健康状态。
- 保持 `SIGNAL_ONLY`、只读数据、禁止自动下单和禁止自动平仓边界不变。

## 当前运行事实

- 配置币种：21。
- 最新扫描状态币种：21。
- OKX WebSocket 订阅：21，连接正常，无降级、无重连错误。
- 15m 闭合 K 线回补：21 个币种完整，写入失败为 0。
- 影子组合：21 个币种可用，0 个跳过。
- 正式 A 级推送仍保持 fail-closed；没有合法当前版本 approved manifest 时不会开放正式推送。

## 版本

- 应用与 Python 包：`3.56.11`
- approved strategy version：`3.56.11`
- strict research identity：`v3.56-strict`

## 验证

发布前执行：

```text
python -m compileall -q src main.py gui.py scripts tests
python -m pytest -q
python scripts/system_check.py source
python scripts/system_check.py runtime --mode observation
python scripts/system_check.py shadow
npm test
npm run build
```

最终结果：

- Python 全量测试：`369 passed, 18 skipped`。
- Python compileall：通过。
- Dashboard：lint、typecheck、21 项测试和 Next.js 生产构建全部通过。
- `system_check.py all --mode observation`：通过；正式 manifest 缺失仅为观察模式警告，正式 A 级推送继续 fail-closed。
- 发布包内容必须与 `RELEASE_FILES.txt` 的 191 项轻量清单完全一致。
- 最终 SHA-256 记录在同名 `.sha256` 文件中。
